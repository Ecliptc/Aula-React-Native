export default function Exercicio4(tipo,base,triangulo,retangulo) {

    let base = (base) * Number(altura) / Number(2);
     Number(base) * Number(altura);
     Number(0);
    return 
        <div>
            A área do {tipo} com base {base} e altura {altura} é {area}.
        </div>
    
}
