export default function Exercicio1( {fahrenheit, numero1, numero2, numero3, celsius})
{
    let fahrenheit, celsius; 
     celsius = Number(fahrenheit) - Number(32) * Number(5) / Number(9)
    return (
        <div>
            
        </div>
    )
}
